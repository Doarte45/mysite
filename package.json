{
  "name": "--typescript",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "npm run lint:paths",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "sync-cv": "node --env-file=.env scripts/sync-cv.mjs",
    "build:fresh": "npm run sync-cv && npm run build",
    "lint:paths": "node scripts/lint-paths.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.95.0",
    "@tailwindcss/vite": "^4.2.4",
    "astro": "^6.2.2",
    "googleapis": "^171.4.0",
    "mammoth": "^1.12.0",
    "tailwindcss": "^4.2.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.9.3"
  }
}
